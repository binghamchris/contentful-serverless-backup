READ-ONLY REVIEW. Do not edit any file. Repo: /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup

You are reviewing the PROJECT'S OWN SPEC, DOCUMENTATION, DEPLOYMENT WORKFLOW and REPO HYGIENE, and producing a gap analysis. Read in full: the three files in `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/.kiro/specs/project-quality-overhaul/` (requirements.md, design.md, tasks.md), all four READMEs (repo root, backup-lambda, filter-lambda, deploy), plus `deploy/.env.example`, `deploy/build-lambda.js`, `.gitignore`, `LICENSE.md`, and `.kiro/hooks/auto-git-commit.kiro.hook`.

Report on:
1. SPEC STATUS: what the project-quality-overhaul spec set out to do, which tasks are ticked in tasks.md, and — by checking actual repo state — which claimed-complete items are NOT genuinely done or only partial. Quote the task lines. Name any requirement with no corresponding task and any unticked task. Also run `cd /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup && git log --oneline -30 && git status --short && git branch -a` and assess the git state: the branch is feat/project-quality-overhaul, and requirements.md, design.md and .kiro/hooks/ are UNTRACKED — so the spec's own requirements and design are not in version control, only tasks.md is. Was this branch ever pushed or merged?
2. DOCUMENTATION ACCURACY: check every factual claim in the four READMEs against the code and template. Specifically verify: does the root README's architecture description match what template.yaml actually deploys? Does deploy/README.md's four-step process actually work (deploy CFN with placeholder code, then push real code from a laptop — note any ordering trap)? Diff the documented CloudFormation parameter list against the template's actual Parameters block, naming any parameter documented-but-absent or present-but-undocumented. Does .env.example match the variables build-lambda.js actually reads? Is the filter-lambda README's spec of the required static-API JSON shape complete enough to implement against?
3. DEPLOYMENT WORKFLOW: critique the model — a laptop zips a directory (including whatever is in node_modules at that moment) and calls lambda:UpdateFunctionCode with credentials from a named profile in a gitignored .env. Cover reproducibility (no npm ci, no lockfile enforcement, stray files and dev deps swept into the zip, no arch targeting for arm64 native modules), no versions or aliases, no rollback, no CI/CD, no dev/prod separation, the stack-update-reverts-code-to-placeholder trap, and that nothing ties deployed code to a commit. Recommend a concrete target approach and state what it costs in complexity.
4. REPO HYGIENE: review .gitignore for gaps (.env, node_modules, zips, coverage). Search for any committed secret or account-specific value (tokens, ARNs, account IDs, bucket names, space IDs) — name the file and describe what you found, but do NOT print anything that looks like a live credential. Check for missing CONTRIBUTING.md, CHANGELOG.md, CODEOWNERS, issue/PR templates, .nvmrc or engines field, editorconfig, lint config, CI. Assess the LICENSE choice (CC BY-NC-SA 4.0) for a SOFTWARE project — Creative Commons explicitly does not recommend CC licences for software; explain the practical consequence for contributors and reuse and name the usual alternatives.
5. RESTORE / RUNBOOK GAP: there is no documented procedure for the thing that matters most — RESTORING from a backup. Is a restore even possible from what is stored (a zip of contentful-export output)? What would the procedure be (contentful-import?), what is NOT captured by the export, and is the backup ever verified? Recommend the documentation and drills needed, and note the absence of any stated RPO/RTO, retention rationale or DR scope.

For EACH finding: title, severity (Critical/High/Medium/Low), file, mechanism, consequence, concrete fix. Be rigorous and complete; this feeds a modernisation plan. Return your findings as the final message.