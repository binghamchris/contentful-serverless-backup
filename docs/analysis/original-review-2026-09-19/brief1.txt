READ-ONLY REVIEW. Do not edit any file. Repo: /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup

You are reviewing the FILTER LAMBDA and the END-TO-END EVENT FLOW for correctness and fragility. Read `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/filter-lambda/index.js` (about 110 lines), `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/filter-lambda/README.md`, `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/README.md`, and the SNS/SQS/Lambda parts of `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/infrastructure/template.yaml`.

Trace the whole trigger chain: AWS Amplify build notification -> SNS topic -> filter Lambda -> SQS FIFO queue (MessageDeduplicationId and MessageGroupId are both the CONSTANT string 'backup', DeduplicationScope: queue, MessageRetentionPeriod 900s) -> backup Lambda. Hunt for concrete defects. Consider at least these and verify each against the code:
1. SUCCEED DETECTION: `message.includes("SUCCEED")` on the raw SNS message body. How brittle is a substring match against Amplify's notification text? Could a FAILED build's message contain the substring (e.g. text naming a previous succeeded build, a branch or commit message containing it)? Could a format change silently stop all backups forever with no alarm? Note that a false negative here is SILENT — no backup, no notification.
2. CONSTANT DEDUPLICATION ID: MessageDeduplicationId is hardcoded to 'backup' for every message. SQS FIFO dedup window is 5 minutes. Work out exactly what this does: which legitimate backup requests get silently DISCARDED (SQS accepts and returns 200 for a deduplicated message), and whether the 'prevent repeated backups' intent is actually achieved or overshoots. Also: with MessageGroupId constant, what does that mean for in-flight serialisation.
3. EXTERNAL HTTP DEPENDENCY: `getLastUpdateTimestamp` calls `fetch(url)` with NO timeout, NO retry, NO status-code check (it calls response.json() regardless of HTTP status), and NO validation of the JSON shape. Enumerate the failure modes: non-200 response, HTML error page, empty JSON object, missing `lastUpdatedAt`, an unparseable date producing Invalid Date. What does `latestUpdate` become, and what does `(dateNow - latestUpdate) / 60000` evaluate to when the date is invalid or undefined? Follow the comparison `dateDiffMins < parseInt(process.env.LAST_UPDATE_WINDOW)` with NaN and state the exact outcome (backup skipped? backup triggered? which?). This is a silent-no-backup path — call it out precisely.
4. ARCHITECTURAL COUPLING: the decision to back up depends on a static JSON API published by the WEBSITE being built. Assess this design: it couples the backup system to the frontend's output format, requires a public URL, and means a frontend change can disable backups. Contrast it with alternatives (Contentful webhooks direct to the backup system, Contentful Sync API change detection, a scheduled EventBridge backup, unconditional backup on build). Recommend.
5. TIME-WINDOW LOGIC: reason about clock skew, the fact that the build takes time so 'last update N minutes ago' is measured at filter time not build time, and whether LastUpdateWindow default 10 is safe. What happens if a content edit lands DURING a build?
6. Only `event.Records[0]` is processed. Can SNS deliver more than one record? What breaks if it does, and what happens if Records is empty/malformed?
7. The handler RETHROWS on error (`throw err`) whereas the backup Lambda returns a 500 object. For an SNS-invoked Lambda, what does a throw do — retries, and does the failure surface anywhere a human sees? Is there an on-failure destination or DLQ for this function? (Check the template.)
8. `console.debug` logging of full URL content and full SQS responses — assess log volume and any leakage.
9. SNS SUBSCRIPTION SCOPE: the template subscribes to ONE topic ARN passed as a parameter, and the deploy README says Amplify creates one topic per branch plus an all-branches topic. What are the consequences of picking the wrong one (e.g. backups triggered by a dev-branch build, or no trigger at all)? Also assess that the SNS topic is NOT managed by this stack.

For EACH finding give: a short title, severity (Critical / High / Medium / Low), the exact file and line region, the mechanism, the user-visible consequence, and a concrete recommended fix. Separate VERIFIED-from-code from INFERRED. Be rigorous and complete; this feeds a project modernisation plan. Return your findings as the final message.