READ-ONLY REVIEW. Do not edit any file. Repo: /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup

You are auditing the TEST SUITE for whether it actually has teeth. The repo added 64 tests in a recent spec push, and the owner needs to know whether they are real protection or theatre. Read EVERY test file under `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/tests/` (15 files across tests/backup-lambda, tests/filter-lambda, tests/deploy, tests/infrastructure), plus `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/package.json` (the test runner is `node --test 'tests/**/*.test.js'` with fast-check and js-yaml as devDependencies) and the source files they claim to test (`backup-lambda/index.js`, `filter-lambda/index.js`, `deploy/build-lambda.js`, `infrastructure/template.yaml`).

For EACH test file determine and report:
1. Does it import and drive the REAL code under test, or does it re-implement the logic inside the test and then assert on its own copy? A test that duplicates the implementation is VACUOUS — it passes regardless of the source. Trace the input to the assertion and say which category it is.
2. Does the assertion depend on the property it claims to test, or is it a tautology (e.g. asserting a constructed object has the field you just put in it)?
3. Are AWS SDK calls mocked, and if so how — and does the mock make the test assert its own mock's behaviour rather than the handler's?
4. Which of the source's real behaviours are NOT covered at all. Specifically check whether anything covers: the warm-container /tmp reuse in the backup handler, the constant SQS MessageDeduplicationId, the untimed fetch and its NaN date path in the filter handler, the returned-500-instead-of-throw behaviour, or the zip-inside-the-zipped-folder ordering.
5. Actually RUN the suite and report the result verbatim: `cd /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup && npm test 2>&1 | tail -40`. Read the output carefully and report the true pass/fail counts, any skipped tests, and how long it takes. Do NOT filter out failure detail.
6. Assess the property-based tests specifically: do the fast-check generators actually explore the risky input space (invalid dates, empty objects, non-200 metadata, unicode, adversarial strings), or are the arbitraries so narrow that only happy paths are generated? Name the properties that are under-constrained.
7. Report what test infrastructure is MISSING: no coverage measurement, no linting (is there any eslint config? check), no CI workflow at all (verify: is there a .github/workflows directory? check with a glob), no integration test against real AWS or a localstack, no CloudFormation linting (cfn-lint), no dependency audit step, no contract test for the Amplify SNS message format or the Contentful export output.

Deliver: a per-file verdict table (file | what it claims | real or vacuous | why), then a ranked list of the most dangerous coverage gaps, then concrete recommendations for a test strategy that would have caught the defects the code actually has. Be specific and unflinching — if a test is worthless, say so and show the line that proves it. Return your findings as the final message.