READ-ONLY REVIEW. Do not edit any file. Repo: /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup

You are reviewing the BACKUP LAMBDA for correctness, reliability and operational defects. Read `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/backup-lambda/index.js` (about 180 lines), `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/backup-lambda/README.md`, `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/backup-lambda/package.json`, and the relevant parts of `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/infrastructure/template.yaml` (Lambda config: arm64, MemorySize 450, Runtime nodejs20.x, Timeout 300; SQS FIFO queue with VisibilityTimeout 1800, MessageRetentionPeriod 900, maxReceiveCount 3, DLQ; EventSourceMapping BatchSize 1).

Trace the handler's real runtime behaviour and hunt for concrete defects. Think hard about at least these, and confirm each by reading the code rather than assuming:
1. WARM CONTAINER /tmp REUSE: the handler writes the Contentful export into the fixed path /tmp/backup and then zips the WHOLE FOLDER with AdmZip.addLocalFolder(inputFolder). Lambda reuses /tmp across invocations on a warm container. Work out precisely what accumulates there across invocations (exported JSON files, downloaded assets, any leftover zip) and what that does to backup size, memory, /tmp exhaustion (default ephemeral storage 512 MB) and correctness (is a backup contaminated with older exports?). State the failure sequence concretely.
2. The zip is written INTO the folder being zipped (zipFilePath is inside localBackupPath) — analyse the ordering and whether a previous invocation's zip gets nested into the next archive.
3. TIMEOUT / VISIBILITY MISMATCH: Lambda Timeout 300s vs SQS VisibilityTimeout 1800s vs MessageRetentionPeriod 900s. Reason about what happens when the export exceeds 300 s (a Contentful space with many assets, downloadAssets: true) — redelivery timing, maxReceiveCount 3, whether the message can expire from the queue before retries are exhausted, and whether a partial/oversized export can loop.
4. MANUAL DeleteMessage vs EVENT SOURCE MAPPING: with a Lambda SQS event source mapping, successful handler return already deletes the message. The code ALSO calls DeleteMessage explicitly, and returns a 500-shaped object (NOT a throw) on failure. Explain the consequences: does a returned 500 cause a retry at all? Is a failed backup silently treated as success by the event source mapping? Is the explicit DeleteMessage redundant, harmful, or a masking of failure? This is central to the user's complaint that they are never notified of failures.
5. MEMORY: MemorySize 450 MB while the code reads the entire zip into a Buffer (fs.readFileSync) and holds it for a single-part PutObject. Assess the scaling ceiling (S3 single PutObject max 5 GB; Lambda payload/memory constraints), and whether streaming or multipart upload is warranted.
6. `new Buffer.from(...)` — is this correct/deprecated JS? What does it actually evaluate to, and is the deprecated Buffer constructor path involved?
7. Returning the raw error object as the response body (`sendResponse(500, err)`) — serialisation and log/PII implications.
8. Contentful export options: `includeDrafts: true`, `downloadAssets: true`, `maxAllowedLimit: 200`, `contentFile`, `exportDir`, `saveFile: true`. Note anything risky or outdated in how they are used (e.g. is deliveryToken needed together with managementToken, does includeDrafts require the preview/CDA token, what does the export write besides the content file).
9. Idempotency and integrity: no checksum, no manifest, no verification the uploaded zip is readable, no record of success anywhere queryable. Also whether a same-second invocation could collide on the S3 key from generateS3Key.
10. Error handling granularity: everything in one try/catch; a failure at any stage returns the same shape and loses which phase failed.

For EACH finding give: a short title, severity (Critical / High / Medium / Low), the exact file and line region, what actually goes wrong (the mechanism), the user-visible consequence, and a concrete recommended fix. Separate what you VERIFIED by reading code from what you INFER about AWS runtime behaviour. Do not speculate about performance numbers you have not measured. Be rigorous and complete; this feeds a project modernisation plan. Return your findings as the final message.