READ-ONLY REVIEW. Do not edit any file. Repo: /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup

You are reviewing the INFRASTRUCTURE-AS-CODE and OBSERVABILITY. Read `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/infrastructure/template.yaml` in full (~250 lines of CloudFormation), plus `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/deploy/README.md` for context.

The owner's stated pain point is: NO MONITORING — he is never notified when a backup fails. Treat that as a first-class deliverable, but review the whole template rigorously.

A) MONITORING GAP — design what is missing, in CloudFormation terms. Confirm from the template that there are ZERO alarms, ZERO alert topics, ZERO metric filters, ZERO dashboards. Then specify the signals that should alarm, each with namespace/metric, dimensions, statistic, period, evaluation periods, threshold and TreatMissingData. Cover at least: backup Lambda Errors, Throttles, Duration near timeout, DLQ ApproximateNumberOfMessagesVisible > 0, SQS oldest-message age, filter Lambda Errors. Then the hard and most important one: DETECTING THE ABSENCE OF A BACKUP. A silent no-backup is the dangerous failure, and a Lambda that never runs emits no Errors metric, so an Errors alarm cannot catch it. Explain concretely how to alarm on 'no successful backup in N days' (a custom/EMF success metric with TreatMissingData: breaching, or a scheduled S3 object-age check), and flag the trap where an alarm on a never-emitted metric with TreatMissingData notBreaching sits pinned OK forever. Recommend whether structured JSON logging/EMF, X-Ray tracing, and Lambda success/failure destinations are worth adding and what each buys.

B) TEMPLATE QUALITY AND SECURITY — check and report on at least:
- Hardcoded physical names blocking a second deployment or forcing replacement: RoleName ContentfulBackupLambdaRole, RoleName AmplifyNotificationFilterLambdaRole, QueueName contentfulBackupQueue.fifo, QueueName contentfulBackupDLQ.fifo, FunctionName, BucketName. Give the concrete consequences.
- NO AWS::Logs::LogGroup resources are declared, so Lambda implicitly creates them with never-expire retention, outside the stack. Cost/hygiene consequence and the fix, including the ordering subtlety.
- The functions deploy with PLACEHOLDER inline ZipFile code, with real code pushed later by a laptop script. Verify whether a subsequent stack update REVERTS the function code to the placeholder, silently disabling backups. Assess severity and recommend the fix.
- IAM least privilege: review each statement. Note logs:CreateLogGroup on `*`, S3 being s3:PutObject only (no AbortMultipartUpload if multipart is introduced), the DLQ Receive/Delete grant (needed?), and the absence of kms:Decrypt for the SecureString SSM parameters — reason carefully about the default aws/ssm key vs a customer-managed key. Flag any over-broad grant.
- S3 hardening. Present: AES256 SSE, versioning, public access block, lifecycle (transition 60d, expire 1095d). Assess what is MISSING: no bucket policy denying non-TLS (aws:SecureTransport), no NoncurrentVersionExpiration/Transition (versioning on, so old versions bill forever), no AbortIncompleteMultipartUpload, no Object Lock/immutability for ransomware resistance, no access logging, no cross-region or cross-account copy (single-region single-account backups share a failure domain with the source), no KMS CMK, no MFA delete. Also check ExpirationInDays 1095 against a GLACIER/DEEP_ARCHIVE transition (minimum-duration charges) and whether a 60-day transition is coherent.
- SQS: MessageRetentionPeriod 900s vs VisibilityTimeout 1800s — coherent? No SSE (SqsManagedSseEnabled/KmsMasterKeyId) on either queue, no queue policy, DLQ has no alarm and no redrive.
- Parameters: no AllowedPattern/MinLength/NoEcho, no constraints on ARNs or the URL, no Metadata/ParameterGroups, LastUpdateWindow unbounded.
- Template-wide: no Outputs, no DeletionPolicy except the bucket, no tagging strategy for cost allocation, no cfn-lint/cfn-guard in the repo.
- Lambda config: arm64, MemorySize 450 (unusual — is it justified anywhere in the repo?), no ReservedConcurrentExecutions, no EphemeralStorageSize despite writing to /tmp, no DeadLetterConfig or EventInvokeConfig on the filter function, no Tracing.

For EACH finding: title, severity (Critical/High/Medium/Low), resource/line region, mechanism, consequence, concrete fix (name the CloudFormation properties; small YAML snippets where useful). Separate VERIFIED-from-template from INFERRED AWS behaviour. Before calling a value a defect, consider whether it might be deliberate. Be rigorous and complete; this feeds a modernisation plan. Return your findings as the final message.