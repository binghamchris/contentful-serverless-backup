READ-ONLY REVIEW. Do not edit any file. Repo: /Users/chrisbingham/Documents/GitHub/contentful-serverless-backup

You are reviewing the INFRASTRUCTURE-AS-CODE and OBSERVABILITY. Read `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/infrastructure/template.yaml` in full (about 250 lines of CloudFormation), plus `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/deploy/README.md` for deployment context.

The repo owner's stated pain point is: NO MONITORING — he is never notified when a backup fails. Treat that as a first-class deliverable, but review the whole template rigorously.

A) MONITORING AND ALERTING GAP — design what is missing, concretely and in CloudFormation terms:
- There are currently ZERO CloudWatch alarms, ZERO SNS alert topics of its own, ZERO metric filters, ZERO dashboards. Confirm this by reading the template.
- Define the specific signals that should alarm, and for each: the metric namespace/name, the dimensions, statistic, period, evaluation periods, threshold, and critically the TreatMissingData setting. Cover at minimum: backup Lambda Errors, Throttles, Duration approaching timeout, DLQ ApproximateNumberOfMessagesVisible > 0, SQS oldest-message age, filter Lambda Errors, and — the hard and most important one — DETECTING THE ABSENCE OF A BACKUP (a silent no-backup is the dangerous failure; a Lambda that never runs emits no Errors metric, so an Errors alarm cannot catch it). Explain how to alarm on 'no successful backup in N days' (e.g. an EMF/custom success metric published by the function with an alarm using TreatMissingData: breaching, or an S3 object-age check on a schedule). Be specific about why the naive alarm does not work.
- Note that an alarm on a metric that is never emitted, combined with TreatMissingData notBreaching, is pinned OK forever — flag any such trap in your own proposal.
- Recommend whether structured JSON logging / EMF, X-Ray tracing, and Lambda success/failure destinations should be added, and what each buys.

B) TEMPLATE QUALITY AND SECURITY REVIEW — check and report on at least:
- Hardcoded physical names that block a second deployment or cause stack-update replacement: RoleName ContentfulBackupLambdaRole, RoleName AmplifyNotificationFilterLambdaRole, QueueName contentfulBackupQueue.fifo, QueueName contentfulBackupDLQ.fifo, FunctionName parameters, BucketName. Explain the concrete consequences (no two environments in one account, update-replacement risk, IAM name collisions).
- NO AWS::Logs::LogGroup resources are declared, so log groups are implicitly created by Lambda with NEVER-EXPIRE retention and are not deleted with the stack. Explain the cost and hygiene consequence and the fix (explicit LogGroup with RetentionInDays, and the ordering subtlety).
- The Lambda functions are deployed with PLACEHOLDER inline ZipFile code and real code is pushed later by a local script — assess this pattern: a stack update REVERTS the function code to the placeholder, silently disabling backups. Verify whether the template would do that on any subsequent deploy and how serious that is. Recommend the fix (S3-based code, or a build pipeline).
- IAM least privilege: review each statement. Note the logs:CreateLogGroup on `*`, the backup role's S3 access being s3:PutObject only (no s3:AbortMultipartUpload — relevant if multipart is introduced), the DLQ ReceiveMessage/DeleteMessage grant (is that needed?), the absence of kms:Decrypt for the SecureString SSM parameters (does GetParameters with WithDecryption need KMS permission — reason carefully about the default aws/ssm key vs a customer-managed key), and whether any wildcard/over-broad grant exists.
- S3 bucket hardening: it has AES256 SSE, versioning, public access block, a lifecycle rule (transition at 60 days, expire at 1095 days). Assess what is MISSING: no bucket policy denying non-TLS requests (aws:SecureTransport), no NoncurrentVersionExpiration or NoncurrentVersionTransition (versioning is on, so old versions accumulate forever and are billed forever), no AbortIncompleteMultipartUpload, no object lock / immutability for ransomware resistance of backups, no access logging, no replication or cross-region/cross-account copy for a BACKUP bucket (single-region, single-account backups share a failure domain with the source), no KMS CMK, no MFA delete consideration. Also check the interaction of ExpirationInDays 1095 with a DEEP_ARCHIVE/GLACIER transition (minimum storage duration charges) and whether transitioning at 60 days to GLACIER-class is compatible with the retention.
- SQS: MessageRetentionPeriod 900s on the main queue (15 min) vs VisibilityTimeout 1800s (30 min) — is that coherent? No SSE (KmsMasterKeyId / SqsManagedSseEnabled) on either queue. No queue policy. DLQ has no alarm and no redrive.
- Parameters: no AllowedPattern/MinLength/NoEcho, no constraints on the ARNs or the URL, no Metadata/ParameterGroups. LastUpdateWindow is a Number with no bounds.
- Missing template-wide items: no Outputs, no stack-level resilience settings, no DeletionPolicy on anything except the bucket, no tags/tagging strategy for cost allocation, no cfn-lint/cfn-guard in the repo.
- Assess Lambda config choices: arm64, MemorySize 450 (an unusual value — is it justified anywhere?), no ReservedConcurrentExecutions, no ephemeral storage (EphemeralStorageSize) setting though the function writes to /tmp, no DeadLetterConfig or EventInvokeConfig on the filter function, no Tracing.

For EACH finding give: a short title, severity (Critical / High / Medium / Low), the resource/line region, the mechanism, consequence, and a concrete fix (with the CloudFormation property names, and small YAML snippets where useful). Separate VERIFIED-from-template from INFERRED AWS behaviour. Before calling a value a defect, consider whether it might be deliberate. Be rigorous and complete; this feeds a project modernisation plan. Return your findings as the final message.