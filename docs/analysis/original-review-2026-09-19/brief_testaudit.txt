READ-ONLY RESEARCH. Do not edit any file. This is a dependency and platform CURRENCY assessment for a Node.js AWS Lambda project. Today's date is 2026-09-19 — use web search, because your training data is older than the current AWS and npm state, and the answers here are time-sensitive.

The project (/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup) is an event-driven Contentful backup on AWS Lambda. Current state, which you should confirm by reading these files: `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/infrastructure/template.yaml` (Runtime: nodejs20.x, Architectures: arm64, on both functions), `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/backup-lambda/package.json` (contentful-export ^7.19.144, adm-zip ^0.5.12), `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/deploy/package.json` (@aws-sdk/client-lambda ^3.567.0, @aws-sdk/credential-providers ^3.568.0, adm-zip ^0.5.12, dotenv ^16.4.5), `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/package.json` (fast-check ^3.22.0, js-yaml ^4.1.1), and the three package-lock.json files (read the top-level dependency versions actually pinned in the locks for backup-lambda and filter-lambda — report the RESOLVED versions, not just the ranges).

Research and report, with a source URL for each factual claim:
1. AWS LAMBDA NODE.JS RUNTIME LIFECYCLE as of 2026-09: which nodejs runtimes are supported, which are deprecated, and the exact published dates for nodejs20.x (deprecation date, block-function-create date, block-function-update date) and for nodejs22.x and any newer (nodejs24.x?). State clearly whether nodejs20.x is already past deprecation TODAY and what that practically means for this stack (can it still be updated? does it still run? what happens on the block dates?). Cite the AWS runtime support policy page.
2. The recommended TARGET runtime for a new deployment today, and the concrete migration considerations from Node 20 to it for this code: any breaking changes in Node 22/24 that affect `require`-based CommonJS, the bundled AWS SDK version in the runtime, global `fetch` availability and behaviour, `new Buffer.from` / deprecated Buffer APIs, and anything about `node --test` (the project uses the built-in test runner).
3. CONTENTFUL-EXPORT: the current published version on npm, whether 7.19.144 is materially behind, whether the package is still actively maintained, and any DEPRECATION or breaking change in it or in the Contentful Management API it uses. Critically: check whether Contentful has changed/retired any API this depends on, whether there are known rate-limit or asset-download changes, and whether Contentful now recommends a different backup/export path (CLI, Sync API, space export in the web app, the newer merge/import-export tooling). Also report any known CVEs or security advisories for contentful-export and its dependency tree.
4. ADM-ZIP: current version, and any security advisories/CVEs for adm-zip (there have been path-traversal/zip-slip issues historically) affecting ^0.5.12. Recommend whether to stay or move to an alternative (e.g. archiver, yazl, node:zlib) and why.
5. AWS SDK v3: how far behind ^3.567.0 / ^3.568.0 are, and whether there are advisories. Note that the Lambda runtime bundles its own SDK version — report which SDK version each candidate runtime bundles and whether this project should bundle its own instead.
6. dotenv, fast-check, js-yaml: current major versions and whether the pinned ranges are behind (fast-check ^3 vs a possible v4 — check).
7. Run `npm audit --omit=dev 2>&1 | tail -30` inside `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/backup-lambda` and again in `/Users/chrisbingham/Documents/GitHub/contentful-serverless-backup/deploy`, and report the REAL output including counts by severity. If a lockfile is missing deps on disk (no node_modules), say so and run `npm audit` against the lockfile only if possible; do not fabricate results. Also report whether node_modules is present in each directory.
8. Recommend a concrete dependency-hygiene setup for this repo: Dependabot/Renovate config, `npm ci` with lockfile, engines field, pinning strategy, and an audit gate in CI.

IMPORTANT REPORTING RULE: separate END-OF-LIFE HYGIENE from LIVE EXPLOITABLE VULNERABILITY. Do not let 'end of life' imply exploitability — state explicitly for each item whether a known CVE affects the pinned version. Cite every date and version claim with a URL. Return your findings as the final message.