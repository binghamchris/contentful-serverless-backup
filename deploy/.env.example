# AWS CLI profile the deploy script assumes (must end with -dev per project convention)
AWS_PROFILE_NAME=your-profile-dev
# CloudFormation stack name — the deploy script reads the three function names
# from this stack's outputs, so they are never hardcoded or duplicated here.
STACK_NAME=contentful-backup
