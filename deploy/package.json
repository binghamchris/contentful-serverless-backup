{
  "name": "contentful-backup-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Reproducible, gated Lambda code deployment via UpdateFunctionCode",
  "engines": {
    "node": ">=24 <25"
  },
  "//dependencyNotes": "All dependencies are on their LATEST versions — see backup-lambda/package.json for the archiver 8 ESM migration note. build-lambda.js uses `const { ZipArchive } = require('archiver')` + `new ZipArchive(...)`. dotenv is on 18.x, verified to still load deploy/.env.",
  "dependencies": {
    "@aws-sdk/client-cloudformation": "3.1136.0",
    "@aws-sdk/client-lambda": "3.1136.0",
    "@aws-sdk/credential-providers": "3.1136.0",
    "archiver": "8.0.0",
    "dotenv": "18.0.1"
  }
}
